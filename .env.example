# Create a .env file from this template and add your own Gemini API key.
GEMINI_API_KEY=

# Optional: override the Gemini model used by the server.
GEMINI_MODEL=gemini-2.5-flash

# Optional: defaults to 3000 when running main.py directly.
PORT=3000
